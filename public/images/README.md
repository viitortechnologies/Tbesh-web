# Site images

**Cloud, DevOps, servers, and software engineering only** — sourced from [Pexels](https://www.pexels.com).

Refresh all assets:

```bash
bash scripts/download-images.sh
```

This removes unused `faq.jpg` and legacy `placeholder-*.svg` files.
